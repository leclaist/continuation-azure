# Default ignored files
/shelf/
/workspace.xml
# Gem library list drifts on every dependency update and isn't meaningful
# to anyone but the IDE that generated it — let it regenerate locally
# instead of tracking it.
/continuation.iml
# Ignored default folder with query files
/queries/
# Datasource local storage ignored files
/dataSources/
/dataSources.local.xml
# Editor-based HTTP Client requests
/httpRequests/
