<% content_for :body_data, "home" %>
<% content_for :head do %>
  <style>
    [data-page="home"] { background-image: url('<%= asset_path("lake-of-the-woods.jpg") %>'); }
  </style>
<% end %>
<h1>Christine Clay Moreau</h1>

<ul class="year-list">
  <% @years.each do |year| %>
    <li><%= link_to year, year_path(year: year) %></li>
  <% end %>
</ul>
